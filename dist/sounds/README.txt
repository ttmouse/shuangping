Put your custom sound files here to override synthesized tones.

Expected filenames:
- correct.mp3  (played on correct input)
- error.mp3    (played on wrong input)  [optional]

These will be fetched at runtime via /sounds/*.mp3.

